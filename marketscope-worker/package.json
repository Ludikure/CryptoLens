{
  "name": "marketscope-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "predeploy": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "vitest": "^1.6.0"
  }
}
